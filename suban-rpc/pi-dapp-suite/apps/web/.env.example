VITE_PI_RPC_URL=http://localhost:8000
VITE_NETWORK_PASSPHRASE=Pi Testnet
VITE_FAUCET_URL=http://localhost:4000

# Optional: prefill the UI admin field (must match faucet ADMIN_TOKEN). Same value ships in the browser bundle — dev only.
# VITE_ADMIN_TOKEN=change-me-to-a-long-random-string

