[workspace]
members = [
  "token",
  "dex_pool",
  "dex_router",
  "subscription",
]
resolver = "2"

[workspace.dependencies]
soroban-sdk = { version = "=21.2.0" }

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true

